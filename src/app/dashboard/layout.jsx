export default function DashboardLayout({ children }) {
    return (
        <section className="flex flex-row w-full h-screen">
            <nav>Test</nav>
            {children}
        </section>
    )
}
